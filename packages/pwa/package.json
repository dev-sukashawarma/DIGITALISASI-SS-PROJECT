{
  "name": "@suka/pwa",
  "version": "0.0.1",
  "description": "PWA utilities for Sukashawarma Outlet Suite — install prompt, SW updater, push helpers",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0"
  }
}
