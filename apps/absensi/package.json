{
  "name": "@ss/absensi",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "M1 — Absensi Outlet + Face Matching (Sukashawarma Outlet Suite)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "fake-indexeddb": "^6.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
